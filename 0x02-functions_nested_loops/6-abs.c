#include "main.h"
#include <stdio.h>
/**
 * _abs - compute the absolute  value of an integer
 * @n: argument
 * Return: Always retunrn interger
 * */
int _abs(int n)
{
	int value = n^2;
	int absol = sqrt(value);
}
