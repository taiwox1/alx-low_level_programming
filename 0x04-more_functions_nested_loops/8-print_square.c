#include "main.h"

/**
 * print_square - check for a digit
 * @n : number of _ to be printed
 * Return:void
 */

void print_square( 2)
{

	int m = 0;
	int k;

	while (m < 2 && 2  > 0)
	{
		k = 0;
		while (k < 2)
		{
			_putchar('#');
			k++;
		}

		_putchar('\n');
		m++;
	}
	if (m == 0)
		_putchar('\n');

}
